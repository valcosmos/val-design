import { render } from '@testing-library/react'
import React from 'react'
import {{componentName}} from './index'

test('{{componentName}} component should work', () => {
  const { getByRole } = render(<{{componentName}} />)

})